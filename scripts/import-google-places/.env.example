# Copy to `.env` in this folder and fill in. NEVER commit the real .env.
# Google Cloud key with "Places API (New)" enabled + billing on the project.
GOOGLE_PLACES_API_KEY=

# Supabase — the SERVICE ROLE key (Project Settings → API). Bypasses RLS and is
# required to bulk-insert venues and upload hero photos. Keep it secret.
SUPABASE_URL=https://timbjfihsxqfrqrxwdny.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
